# Plugin Factory

Plugin Factory — это лёгкая и расширяемая система плагинов для Python‑приложений.  
Она позволяет автоматически обнаруживать, загружать и управлять плагинами без изменения основной логики приложения.

---

## 🚀 Возможности
- **Динамическое обнаружение плагинов** в файловой системе
- **Автоматическая загрузка модулей**
- **Строгая валидация классов плагинов** через интерфейс `IPlugin`
- **Безопасное определение классов** (никакого `__subclasses__()`, только классы из модуля)
- **Управление жизненным циклом плагинов**: запуск, остановка, массовое управление
- **Простая архитектура**, лёгкая интеграция в любой проект

---

## 📁 Структура проекта
```
project/
│ plugin_loader.py      # Логика загрузки и инициализации плагинов
│ plugin_manager.py     # Управление плагинами
│ IPlugin.py            # Базовый интерфейс
│ __init__.py           # Общие настройки
│
└── plugins/            # Каталог пользовательских плагинов
```

---

## 🧩 Интерфейс IPlugin
Все плагины должны реализовывать интерфейс `IPlugin`:

```python
from abc import ABC, abstractmethod

class IPlugin(ABC):
    @property
    @abstractmethod
    def name(self) -> str: ...

    @property
    @abstractmethod
    def status(self) -> str: ...

    @abstractmethod
    def start(self) -> None: ...

    @abstractmethod
    def stop(self) -> None: ...
```

### Требования
- `name` — уникальный идентификатор
- `status` — состояние плагина
- `start()` — выполняется при запуске
- `stop()` — выполняется при остановке

---

## 🔍 PluginLoader
Компонент, который отвечает за поиск, загрузку и инициализацию плагинов.

### Использование
```python
from plugin_loader import PluginLoader
loader = PluginLoader()
plugins = loader.load()
```

### Как работает загрузка
1. Поиск папок внутри `plugins/`
2. В каждой папке поиск файлов:
   ```
   plugin_*.py
   ```
3. Импорт модулей
4. Поиск классов:
   - наследуются от `IPlugin`
   - не являются абстрактными
   - объявлены в текущем модуле
5. Создание экземпляров плагинов

---

## ⚙️ PluginManager
Управляет жизненным циклом всех загруженных плагинов.

### Использование
```python
from plugin_manager import PluginManager
manager = PluginManager(plugins)

manager.start("logger")
manager.stop("logger")

manager.start_all()
manager.stop_all()

manager.get_status()
```

### Функции
- Запуск и остановка одного плагина
- Массовый запуск и остановка
- Получение статусов всех плагинов

---

## 📝 Пример плагина
Файл:
```
plugins/logger/plugin_logger.py
```

```python
from IPlugin import IPlugin

class LoggerPlugin(IPlugin):
    @property
    def name(self) -> str:
        return "logger"

    @property
    def status(self) -> str:
        return "ready"

    def start(self) -> None:
        print("Logger started")

    def stop(self) -> None:
        print("Logger stopped")
```

---

## 🧱 Расширение
Вы можете расширить систему:
- метаданными плагинов
- категориями и типами плагинов
- горячей перезагрузкой
- зависимостями между плагинами
- версионированием плагинов

---

## 📄 Лицензия
MIT License (или укажите свою)

---

## 💬 Автор
Plugin Factory — created by Yaroslav Volkotrub

---